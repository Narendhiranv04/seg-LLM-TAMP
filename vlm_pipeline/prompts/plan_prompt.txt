{STATE_TEXT}

## GOAL:
{GOAL_TEXT}

## VISUAL CONTEXT:
The attached image shows 5 camera views of the kitchen scene:
- Top row: left shoulder view, right shoulder view, overhead view
- Bottom row: wrist camera (end-effector), front view

Use the visual information together with the segmentation observation to plan the action sequence.
Use raw object names exactly as they appear in the observation.

## KNOWN REGIONS:
- placement_boundary
- cupboard_boundary
- cupboard_boundary_top
- groceries_boundary
- box_boundary
- table

## EXAMPLE PLAN (for reference):
For goal "Move mug2 to placement_boundary, open box lid if needed, and move soup to cupboard_boundary":
1. move(mug2)
2. pick(mug2)
3. move(mug2, placement_boundary)
4. place(mug2, placement_boundary)
5. move(soup)
6. pick(soup)
7. move(soup, cupboard_boundary)
8. place(soup, cupboard_boundary)

Now output YOUR action sequence for the given goal:
